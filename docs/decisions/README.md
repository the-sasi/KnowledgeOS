# docs/decisions

Architecture Decision Records. One file per decision, numbered in order,
never rewritten — superseded by a later ADR instead.

Naming: `0001-short-title.md`

Start from `0000-adr-template.md`.

Decisions still open: database, vector database, LLM provider, embedding
model, application framework, web framework, agent orchestration,
observability stack.
