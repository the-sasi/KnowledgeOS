# docs/architecture

System design documents: component boundaries, data flow, and the reasoning
behind the module split.

To write as the platform takes shape:

- `overview.md` — components and how they fit together
- `data-flow.md` — SEC filing to answer, end to end
- `module-boundaries.md` — what each service owns and what it must not touch

Empty by design.
