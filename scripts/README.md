# scripts

Developer and operational helper scripts — thin wrappers only. Real logic
belongs in `services/`.

Expected over time: environment bootstrap, ingestion triggers, index rebuilds,
evaluation runs, cleanup.

Keep scripts runnable inside the Docker environment.

Empty by design.
