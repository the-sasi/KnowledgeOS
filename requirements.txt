# KnowledgeOS - runtime dependencies
#
# Deliberately minimal. No ORM, no migration framework, no LLM/embedding
# libraries yet - those arrive with the modules that need them.

psycopg[binary]==3.2.3    # PostgreSQL driver
requests==2.32.3          # SEC EDGAR HTTP client
beautifulsoup4==4.12.3    # HTML parsing
lxml==5.3.0               # fast, lenient HTML/XML tree builder
