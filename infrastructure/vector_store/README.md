# infrastructure/vector_store

Home for the vector index infrastructure: container definition, collection
setup, and index configuration.

Persisted index data belongs in `data/indexes/`, not here.

Not configured. No vector database has been chosen.
