# infrastructure/database

Home for the metadata/document store: container definition, schema,
migrations, and seed scripts.

Not configured. No database engine has been chosen. Record the decision as an
ADR in `docs/decisions/` before adding anything here.
