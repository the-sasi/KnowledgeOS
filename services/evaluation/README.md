# services/evaluation

Measures whether the system actually works.

Scope (not implemented):

- evaluation datasets and golden answers
- retrieval metrics (recall, precision, ranking quality)
- answer quality and groundedness scoring
- regression tracking across changes

Test harnesses live in `tests/evaluation/`; results are written to
`data/generated/`.
